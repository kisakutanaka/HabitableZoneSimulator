# Step.md — 実装ロードマップ

science-app-framework.mdの3点セット方針に基づく実装ロードマップ。フェーズごとにチェックボックスで進捗管理し、実装後は各ステップに一言メモ（何を作ったか、何が分かったか）を追記していく。

## 合意事項

- **恒星パラメータUI**: 単一スライダー（恒星質量）で光度・表面温度・半径を主系列関係から連動して算出する。光度・表面温度は数値として表示する。
- **流用機能の扱い**: `main.ts`にある対数表示切替・回転中心（基準天体）切替／逆行軌道・距離ルーラーUIは、Plan.mdの企画に含まれずUIをシンプルに保つ方針にも合わないため、Phase 0で削除する。
- **デプロイ方針**: 早期にGit初期化＋GitHub Pages設定を行い、以降の各フェーズで実機確認する。GitHubリポジトリは作成済み（`git@github.com:kisakutanaka/HabitableZoneSimulator.git`）。Phase 0でこれを`origin`に設定し、Viteの`base`パスは`/HabitableZoneSimulator/`とする。
- **表示する天体**: Phase 1でSun以外の7惑星を削除し、太陽・ハビタブルゾーン・地球のみのシンプルな構成に変更（当初案の「8惑星表示」から縮小）。恒星パラメータ変更→HZ変化、地球公転半径変更→気温変化という中核体験に画面を集中させるため。

## 科学的モデルの方針

- **恒星モデル**: 質量 M/M☉ を単一パラメータとし、主系列近似で
  - L/L☉ ≈ (M/M☉)^3.5
  - R/R☉ ≈ (M/M☉)^0.8
  - Teff = T☉ · (L/L☉)^0.25 / (R/R☉)^0.5（Stefan-Boltzmann）
  から光度・半径・表面温度を連動計算する。スライダー範囲はTeffがKopparapu式の検証範囲（2600K〜7200K）に収まるよう概ね0.4〜1.6 M☉に制限する（実装時に再確認）。
- **ハビタブルゾーン**: Kopparapu et al. (2013) の実効フラックス多項式から内側・外側境界を計算する（TeffとLに依存）。太陽のデフォルト値（M=1）でHZが公表値（概ね0.95〜1.7AU前後）と一致することをFindings.mdで検証する。
- **地球の気温**: 放射平衡温度 Teq = Teff·√(R★/(2a))·(1-A)^0.25（アルベドA=0.3固定、aは軌道長半径）。デフォルト（M=1, a=1AU）で約255K（-18℃）という教科書値と一致することをFindings.mdで検証し、UI上は「放射平衡温度」であり大気による温室効果は含まない旨を明記する。
- **地球の公転周期**: ケプラー第3法則 Torbit = √(a³/M)（AU・太陽質量・年単位）で、軌道半径スライダーと恒星質量スライダーの両方に連動させる。他の7惑星の公転周期は簡略化のため据え置き（本アプリの教育目標はハビタブルゾーンと地球環境の変化であり、他惑星の軌道の物理的整合性はスコープ外）。

## フェーズ

### Phase 0: プロジェクトセットアップ
- [x] `git init`（デフォルトブランチをmainに変更）
- [x] Vite + TypeScript構成を追加（package.json, tsconfig.json, vite.config.ts, index.html）。three・@types/threeを依存関係に追加
  - メモ: 流用元main.tsが使っていた`THREE.Timer`は`three@0.169`にまだ存在しないAPIだったため、`THREE.Clock`に置き換えた。
- [x] `src/main.ts`から対数表示切替・回転中心切替・逆行軌道・距離ルーラーの関連コードを削除し、8惑星＋軌道線＋自由視点カメラのみのシンプルな3Dシーンに整理
  - メモ: Phase 1で「地球以外の7惑星は不要では」との判断により、太陽・地球のみの構成にさらに縮小（下記Phase 1参照）。
- [x] 作成済みのGitHubリポジトリ（kisakutanaka/HabitableZoneSimulator）をoriginに設定してpush。GitHub Actionsでbuild&deployするワークフローを用意し、GitHub Pages公開設定を行う
  - メモ: GitHub Pages公開設定（Settings→Pages→Source: GitHub Actions）はユーザーが手動で設定。Actionsのデプロイは初回から成功。
- [x] 実機確認: `npm run dev`でローカル動作確認（Playwrightでスクリーンショット・console/pageerror確認、エラーなし）。本番ビルド（`npm run build`+`vite preview`）でもbaseパス配信を確認。公開URL https://kisakutanaka.github.io/HabitableZoneSimulator/ もPlaywright（モバイル解像度）でエラーなしを確認済み。iPhone実機での最終目視確認はユーザー側で実施予定。

### Phase 1: ハビタブルゾーンの計算・表示（太陽固定）
- [x] 地球以外の7惑星（水星〜海王星、火星より外側含む）を表示から削除し、太陽・地球のみの構成に変更
  - メモ: ユーザー判断により、UIをハビタブルゾーンと地球環境の変化という中核体験に集中させるため。カメラ距離（旧: 惑星系全体を見る想定）と`OrbitControls`の`maxDistance`も新しいシーンスケールに合わせて縮小した。
- [x] `src/star.ts`を新設し、恒星質量→L・R・Teff、L・Teff→HZ内側/外側AU（Kopparapu式）の計算関数を実装
- [x] HZを太陽面（y=0平面）の半透明なリング（RingGeometry）として3Dシーンに追加
- [x] 公表されている太陽のHZ値と突き合わせて検証し、Findings.mdに記録
  - メモ: Kopparapu et al. (2013)のerratum記載の係数を一次情報で確認して実装。太陽（M=1）でHZ=0.981〜1.689AUとなり、公表値（約0.99〜1.70AU）とほぼ一致。地球（1.0AU）はHZ内側境界のすぐ外側＝HZ内に位置することを確認。
- [x] 実機確認: `npm run dev`でPlaywright screenshot確認（太陽・HZ帯・地球が意図通り描画、console/pageerrorなし）。`npm run build`も成功。

### Phase 2: 恒星パラメータ変更UI（質量スライダー）
- [x] 質量スライダー（0.5〜1.5 M☉、Kopparapu式の有効温度範囲に収まるよう設定）と、光度・表面温度の数値表示を追加
- [x] 太陽メッシュの表示半径（ベース半径×R★のスケール比）と色（Teffからの黒体色近似）、点光源強度（Lに比例）をスライダーに応じて更新
  - メモ: 惑星サイズの立方根圧縮則は今回は不要（太陽のみで、Earth他惑星との相対サイズ比較がなくなったため）。単純にR★をスケール比として使用。
- [x] スライダー変更に応じてHZリングをリアルタイム再計算・再描画（RingGeometryを都度dispose&再生成）
- [x] 検証: M=1でPhase 1の太陽値と一致、M=0.5→HZが太陽近くまで縮み地球がHZ外（寒すぎ側）、M=1.5→HZが大きく広がり地球がHZ内側（暑すぎ側）になることをPlaywrightで確認。詳細はFindings.md参照。
  - メモ: 太陽の色は黒体近似式で計算するとM=1でほぼ白（イラスト的な黄色ではない）になった。天文学的に正しい（宇宙から見た太陽は白色）ため、そのまま採用する方針にユーザーと合意。
- [x] 実機確認: Playwrightでモバイル解像度（390×844）でも操作パネルが3D表示と重ならずスクロールなしに収まることを確認、console/pageerrorなし。本番ビルドも成功。

### Phase 3: 地球の公転半径変更UI＋気温シミュレーション
- [ ] 地球の軌道半径スライダー（例: 0.5〜2.5AU）を追加
- [ ] 地球メッシュ・軌道線の半径をスライダー値に更新し、ケプラー第3法則で公転周期（アニメーション角速度）も再計算
- [ ] 放射平衡温度を計算して数値表示し、現在のHZ範囲内か外（暑すぎ/寒すぎ）かを示すインジケーターを追加
- [ ] 検証: デフォルト値（M=1, a=1AU）で約255K（-18℃）になることを教科書値と突き合わせてFindings.mdに記録
- [ ] 実機確認

### Phase 4: UI統合・モバイル1画面レイアウト調整
- [ ] 2つのスライダーと数値表示を1つのコンパクトな操作パネルにまとめ、各スライダーに短いヒント文言を添える
- [ ] モバイル縦画面でスクロールなしに主要体験（3D＋両スライダー）が収まるか確認・調整
- [ ] 実機確認（iPhone実機で最終レイアウト確認）

### Phase 5: 教育目標との対応確認・仕上げ
- [ ] Plan.mdの3つの教育目標それぞれに、UIのどの要素が対応しているかを明示的に確認・整理
- [ ] Findings.mdの内容を見直し・整理
- [ ] 最終デプロイ＋実機確認

## 主な変更/新規ファイル

- `package.json` / `tsconfig.json` / `vite.config.ts` / `index.html`（新規、Viteスキャフォールド）
- `src/main.ts`（既存を大幅簡略化: 対数/回転中心/ルーラー関連を削除し、HZリング・恒星スライダー・地球軌道スライダーの配線を追加）
- `src/planets.ts`（既存データ構造をそのまま流用、変更なし）
- `src/star.ts`（新規: 恒星物理量・HZ・地球気温の計算ロジック）
- `Findings.md`（新規、3点セットの残り1つ）
- `.github/workflows/deploy.yml`（新規、GitHub Pages自動デプロイ）

## 検証方法

- 各フェーズ完了時に `npm run dev` でブラウザ起動し、コンソールエラー・pageerrorがないか確認（可能であればPlaywrightで自動チェック）
- `npm run build && npm run preview` で本番ビルドの動作を確認してからデプロイ
- Phase 0以降、各フェーズ末尾でGitHub Pagesの公開URLをiPhone実機で開き、タッチ操作・レイアウトを確認
- 恒星モデル・HZモデル・気温モデルの数値は、太陽のデフォルト値において公表値／教科書値と突き合わせ、差異があれば原因をFindings.mdに記録
