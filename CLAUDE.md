# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

「ハビタブルゾーンシミュレータ」は、恒星のパラメータを変化させることでハビタブルゾーン（恒星周辺で惑星表面に液体の水が存在できる範囲）がどう変わるかを体験できる、ブラウザのみで動作する教育用Webアプリ。ScienceMuseum系の教育コンテンツの一つ。企画・教育目標・UXフローの詳細は [Plan.md](Plan.md) を参照。

### 教育目標（Plan.md より）
- 地球がハビタブルゾーンに位置することを知る
- ハビタブルゾーンの条件を知る
- 条件が変わると惑星の環境がどう変わるか知る

### ユーザー体験の流れ（Plan.md より）
1. 線形尺度の太陽系（惑星・軌道・ハビタブルゾーン）を3Dで表示
2. 自由視点操作でいろんな角度から見られる
3. シンプルなUIで恒星のパラメータを変更できる
4. 恒星パラメータの変更でハビタブルゾーンがどう変化するか見られる
5. シンプルなUIで地球の公転半径を変更できる
6. 公転半径の変更で地球の気温などがどう変化するか見られる

### 設計方針（Plan.md より）
- ブラウザのみで動作する（GitHub Pages対応、サーバーサイド処理に依存しない）
- iPhoneでの動作を想定（モバイル・タッチ操作前提）
- TypeScriptベース
- 教育効果を最優先する。UIはできるだけシンプルにし、不要な機能や華美な装飾は追加しない

## 開発フレームワーク

このリポジトリは [science-app-framework.md](science-app-framework.md) に記載された、ScienceMuseum系教育アプリ共通の開発プロセスに従う（GWPlayer＝重力波プレイヤーの開発から一般化されたもの）。実装・変更に着手する前に必ず目を通すこと。要点:

- **3点セットのドキュメント方針**: CLAUDE.md（このファイル、ほぼ不変の指針）／Step.md（実装ロードマップ。フェーズごとにチェックボックスで進捗管理し、実装後は各ステップに一言メモを追記）／Findings.md（開発中に得た科学的な検証結果・発見のログ）。**Step.mdとFindings.mdはまだ作成されていない** — 実装フェーズに入る際に用意すること。
- **縦を先に通す**: 複数パターン対応などの横展開に着手する前に、まず1パターンでMVPを一気通貫させて動作確認する。
- **フェーズごとに実機確認**: まとめて最後に確認せず、フェーズ完了ごとに早期デプロイ→実機確認のサイクルを回す。
- **科学的正しさの検証**: 自前実装した計算式は公表値や公式APIの値と突き合わせて検証し、差異があれば原因（丸め誤差・定義の違いなど）を特定してFindings.mdに記録する。
- **実アプリでのテスト**: モックではなく実際に起動してブラウザで動作確認する（Playwright等）。スクリーンショット・コンソールログ・pageerrorを能動的に確認し、疑わしい挙動は指摘される前に自分で深掘りする。
- **UIの型**: 「体験」（結果を直感的に見る）と「仕組みの理解」（解説）を分離し、まず体感してもらってからスクロールで解説という流れが有効。選択肢UI自体（`<option>`など）にも短いヒントを入れる。モバイル一画面に主要な体験が収まるか早めに確認する。
- **教育目標との対応付け**: 実装終盤で「UIのどの部分がどの教育目標に対応しているか」を確認するフェーズを設ける。機能を足すか迷ったらPlan.mdの教育目標に立ち返って判断する。

## 現在の実装状況

まだビルド設定（package.json / tsconfig / index.html 等）が存在せず、`src/` 以下のTypeScriptのみが置かれている段階。npmスクリプトやテストコマンドは未整備。セットアップから着手する場合は science-app-framework.md の「ブラウザのみで完結」「モバイルで動作」の方針に沿ったビルドツール（Vite等）を選定すること。

[src/main.ts](src/main.ts) と [src/planets.ts](src/planets.ts) は、実装が似ている別アプリ（太陽系の惑星軌道を線形/対数スケールで切り替え表示する3Dビジュアライザー）から持ち込まれたコード。ハビタブルゾーンの表示や恒星パラメータUIはまだ実装されていないため、このコードは「そのまま使えるベース」ではなく「流用元」として扱う。特に以下は本アプリでも再利用しやすい:

- Three.js + OrbitControlsによる3Dシーンのセットアップ（`main.ts` 冒頭）
- `planets.ts` の惑星データ構造（距離AU・半径km・公転周期年）
- 惑星の見た目サイズを誇張するための立方根スケーリング（距離は誇張しないというルール）
- 線形/対数スケール切り替えとそのブレンドアニメーション（`applyScaleBlend` まわり）
- 軌道線をCatmull-Romスプラインで滑らかに描く仕組み

一方、回転中心の切り替え（`center-select`）や逆行軌道の可視化は太陽系スケール比較アプリ固有の機能で、Plan.mdの企画には含まれていないため、そのまま残すかは要検討。ハビタブルゾーンの計算・表示ロジック、恒星パラメータ変更UI、地球の公転半径変更UI、地球の気温変化の表現はまだコード上に存在せず、これから実装が必要。
